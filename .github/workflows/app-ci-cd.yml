# name: App CI/CD Pipeline (GitOps)

# on:
#     push:
#         branches: ["main"]
#         paths:
#             - "src/**" # Imagine your app source code is here

# env:
#     AWS_REGION: ap-south-1
#     ECR_REPOSITORY: my-app-repo

# jobs:
#     build-and-push:
#         runs-on: ubuntu-latest
#         steps:
#             - name: Checkout Code
#               uses: actions/checkout@v3

#             - name: Configure AWS credentials
#               uses: aws-actions/configure-aws-credentials@v2
#               with:
#                   aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#                   aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#                   aws-region: ${{ env.AWS_REGION }}

#             - name: Login to Amazon ECR
#               id: login-ecr
#               uses: aws-actions/amazon-ecr-login@v1

#             - name: Build, tag, and push image to Amazon ECR
#               env:
#                   ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#                   IMAGE_TAG: ${{ github.sha }}
#               run: |
#                   # Build your docker image (Assuming Dockerfile is in src/)
#                   # docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG ./src
#                   # docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

#                   # For this example, we'll pretend we built and pushed it.
#                   echo "Pushed image $IMAGE_TAG"

#             - name: Update Kubernetes manifests (GitOps)
#               env:
#                   IMAGE_TAG: ${{ github.sha }}
#               run: |
#                   # Use sed to update the image tag in our Kubernetes deployment file
#                   sed -i "s|image: nginx.*|image: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG|g" k8s/sample-app.yaml

#                   git config --global user.name 'GitHub Actions'
#                   git config --global user.email 'actions@github.com'
#                   git add k8s/sample-app.yaml
#                   git commit -m "Update image tag to $IMAGE_TAG (GitOps deployment)"
#                   git push

#                   # AFTER Git push, ArgoCD running inside EKS will automatically 
#                   # detect this change in the GitHub repo and sync the cluster!
